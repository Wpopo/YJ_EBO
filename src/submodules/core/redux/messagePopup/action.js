import { createAction } from 'redux-actions';

export const setContent = createAction('SET_CONTENT');
export const setParams = createAction('SET_PARAMS');
export const clearParams = createAction('CLEAR_PARAMS');
